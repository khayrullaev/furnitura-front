// import React, { createContext, useState } from "react";
// import styled from "styled-components/native";
// import { View } from "react-native-animatable";

// // styles
// import { theme } from "../styles";

// interface BottomSheetInterface {
//     toggleSheet: (visible: boolean) => void;
// }

// export const BottomSheetContext = createContext<BottomSheetInterface | any>(undefined);

// export const BottomSheetProvider:  React.FC = ({children}) => {
//     const [visible, setVisible] = useState<boolean>(false)

//     const toggleBottomSheet = (visible: boolean) => setVisible(visible);

// }
